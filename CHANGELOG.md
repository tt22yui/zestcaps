# 更新日志

本文件记录 ZestCaps 各版本的更新内容。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.3.4] - 2026-09-11

截图/钉屏与配置健壮性修复，测试与发布链路加固。

### 修复

- **钉屏后看不到边框**：边框是独立顶层窗口，`Gui.Move` 不会让隐藏窗口显现（实测 Move 后仍不可见），
  而「新建边框后只 Move」的路径（编辑器直接进入、独立钉屏）从未显式显示过边框；现由 `BorderStripsMove` 统一补显。
- **最小化后残留边框**：边框/工具栏等覆盖层是独立顶层窗口，不跟随宿主最小化，
  钉屏或编辑窗最小化后桌面上会残留覆盖层；现按宿主 `WM_SIZE`（最小化/还原）同步隐藏与恢复。
- **截图保存提示错误**：修正 GDI+ 保存接口的返回值判定（0=成功、负值=失败），此前每次保存成功都会误报「截图保存失败」，而真正失败时无提示。
- **自动更新失败后程序退出**：更新失败（校验不符 / 下载异常）时未取消 30 秒下载看门狗，导致失败约 30 秒后进程被超时回调退出。
- **配置值异常导致启动失败**：`config.ini` 中保留天数为非数字（或程序目录不可写）时，启动阶段会弹出错误框且热键/托盘全部失效；现改为回退默认值并继续启动。
- **设置页保存无响应**：保留天数/执行时刻填入非数字时，「保存并重启」会静默失效（异常被全局兜底吞掉），现改为明确提示；清空时刻增加 00:00 - 23:59 范围校验。
- **本地构建静默失败**：`build.bat` 此前只打印 Ahk2Exe 退出码而不判定，编译失败但上次产物仍在时会误报成功并返回 0；现在编译前删除旧产物并透传非零退出码。
- **截图马赛克**：位图锁定失败时不再继续按空指针取色（可能崩溃），改为跳过该格。
- **画笔/马赛克末段缺失**：松开鼠标前的最后一段笔画此前不绘制，现追记末点。
- **就地钉屏句柄泄漏**：编辑窗就地钉屏未释放画笔/马赛克圆圈光标句柄，每次泄漏一个 USER 句柄。
- **编辑器保存框按 Esc**：在系统保存对话框里按 Esc 取消保存，会连带把整个编辑会话取消；现对话框期间临时停用 Esc。
- **窗口竞态**：悬停/选区同步在窗口消失时不再抛出异常中断截图会话（AHK v2 对已销毁窗口抛 TargetError）。
- **截图不可用时的降级**：GDI+ 初始化失败只停用截图功能，不再弹框并退出整个程序。
- **关闭截图后 F1 变死键**：功能关闭时仍注册截图热键并把按键吞掉（不透传），F1 在游戏/帮助里按不动；现关闭时不再注册该热键，恢复系统原生行为。
- **自动更新的版本号解析**：此前无条件截掉 tag 首字符（`SubStr(tag,2)`），非 `v` 前缀的 tag（如 `0.4.0`）会被解析成 `.4.0` 导致版本比较错乱；现只去 `v`/`V` 前缀。
- **更新包校验文件读取**：sha256 文件为 0 字节或首行为空时会抛异常（被误报为「下载失败」），且按单空格切分会把文件名并进哈希；现按任意空白取首个字段，空内容安全返回空串。
- **选区双击判定跨会话残留**：双击状态跨会话保留，上一次截图结束时的按下若落进双击时间/位移窗，新会话首次单击平移会被误判为「双击→直接复制」；现每次进入微调前复位。
- **拖动丢失鼠标松开消息**：钉屏/编辑窗拖动时若 `WM_LBUTTONUP` 丢失（被其他程序抢走或系统卡顿），窗口会一直黏着鼠标且 10ms 定时器空转；现检测到左键已弹起即按「松开」收尾。
- **钉屏分层窗口绘制**：GDI 句柄创建失败时不再拿 0 句柄继续 SelectObject（窗口保持上一帧，不崩不闪）。
- **钉屏关闭的窗口竞态**：右键关闭单个钉屏、Esc 关闭全部钉屏时，窗口若在存在性检查与关窗之间销毁会抛 `TargetError`，导致「关闭全部」循环漏关其余钉屏；现逐个包 try。
- **就地钉屏漏提交文本**：钉屏前若文本输入会话仍活跃（覆盖窗未及失焦提交），该段文字不会进入钉屏画面；现钉屏前先提交。
- **编辑窗拖动异常刷屏**：拖动定时器里移动窗口/覆盖层若抛异常（覆盖层已被销毁），会每 10ms 被全局兜底记一条日志；现吞掉该异常并按「已应用」记录，不刷屏。
- **工具栏悬停动画的安全网失效**：`HoverEaseTick` 里检测「工具栏已销毁」时先读了 `btn.Hwnd`，而 AHK v2 对已销毁控件取 `Hwnd` 本身就会抛 `The control is destroyed`，安全网形同虚设；现整体 try 兜底，异常一律按「已销毁」处理（停定时器 + 清 Map，不向外抛）。

### 工程

- CI 新增全链路语法校验门禁（`test\Load\validate_main.ahk` + `/validate`，覆盖 Main.ahk 全部 `#Include`）与测试输出回显、单测报告上传。
- CI 校验 tag 与 `APP_VERSION` 一致性，避免版本号漂移导致客户端反复提示更新。
- CI 改用官方 Zip 内的 `Compiler\Ahk2Exe.exe` 直接编译，移除第三方编译 action 与其 `GITHUB_TOKEN` 依赖；补充 `concurrency`、`timeout-minutes`。
- 语法门禁升级为 `#Warn All, StdOut`（此前只开「变量未赋值」一类）：全链路 17 处告警清零，此后任何新告警（拼错变量名、局部与全局同名、不可达代码等）都会让构建失败。
  - 顶层（自动执行段）的 `catch as err` 会创建全局 `err`，使各处函数内同名局部被判为「与全局同名」——改为独立变量名（`Config.ahk` / `Splash.ahk`）。
  - `EditorLButtonDown` 漏声明 4 个拖动目标全局，起点坐标实际写进了局部变量（顺带修掉声明）。
  - `Gdip_All_v2.ahk` 中与内置 `Format` / `String` 同名的局部变量改名（本地适配，已在注释标注）。
- 死代码清理：删除空实现却被调用 7 处的 `EditorReleaseAnnotationCache`、只写不读的 `EditorToolLabels`、无调用方的 `ShowPin`、空函数 `Screenshot_OnExit`。
- `build.bat` 健壮性：`APP_VERSION` 解析失败改为报错退出（不再静默降级成 `zestcaps_vdev.exe` 却算构建成功）；回显 Ahk2Exe 日志时关闭延迟展开（此前日志里的 `!` 会被吞掉）。
- 指标器退出时配对 `timeEndPeriod`（此前只调 `timeBeginPeriod`，本进程生命周期内持续抬高系统计时精度）。
- `.gitignore` 精简为项目实际需要的规则（去掉继承自 Java/Maven 模板的 `target/`、`.classpath`、`nbproject/` 等无关段）。
- 补 CHANGELOG 缺失的历史小节：[0.3.1]（该版本未推送 tag，内容随 0.3.2 发布）、[0.3.0]、[0.1.1] 及对应链接。
- `tmp/` 清理：删除 35 个一次性产物（构建日志、马赛克/箭头探针输出、图标探针等，约 1.3 MB）；把仍有长期价值的「输入卡死常驻探针」提升为 `test\Diagnostics\test_input_freeze_probe.ahk`（可版本化、含完整用法与产物说明）。
- `test\lib\Yunit\README.md` 补充许可说明（AGPL-3.0，含规范文本地址与下载命令；本环境无外网未能随附全文）与本地适配清单。
- `test\Load\test_compile_load.ahk` 头部补充与 `validate_main.ahk` 的分工说明（静态 `/validate` 门禁 vs 执行型加载检查）。
- 移除 `src\config.ini` 中已无代码读取的废键 `[Screenshot] ScreenshotPath`。
- 钉屏缩放的 DPI 上下文假设写入代码注释（多显示器 + 高缩放需实机验证后再统一，未验证前不贸然改坐标口径）。
- 新增 `test\Updater\test_unit_updater.ahk`（Release JSON 解析 / sha256 文件读取 / SHA256 计算）。
- 新增 `test\RecycleBin\test_unit_recyclebin.ahk`（保留期判定纯函数 `RecycleBinShouldDelete`，含「拿不准一律保留」的安全边界）。
- 新增 `test\Screenshot\test_unit_dblclick.ahk`（选区双击判定与跨会话复位）；`test\Load\test_main_load.ahk` 失败时改为非零退出。
- 单测输出已清零告警（含 `JUnit.ahk` 适配层局部变量与内置 `File` 类同名问题）。
- 新增 GUI 回归测试（重构 `ShowEditor`/`SelectRegion`/`HoverEaseTick` 等长函数与全局状态前的护栏），
  并加聚合运行器 `test\run_gui_tests.ahk`（本地一条命令跑完 9 个窗口类测试，可交互桌面下运行）：
  - `test\Screenshot\test_editor_lifecycle.ahk`（36 项）：编辑窗打开期现状（窗口/两行工具栏/蒙版/4 条边框可见、
    4 个位图与尺寸缩放）→ 复制路径收尾（窗口销毁、位图/标注/工具栏/蒙版/边框/圆圈光标全释放、全局复位、
    Esc 需求归零、剪贴板确实写入位图）→ 就地钉屏交接（会话注册、边框保留且可见、蒙版与工具栏销毁、
    圆圈光标句柄释放、关闭后会话与边框清理）。
  - `test\Screenshot\test_selection_cancel.ahk`（17 项）：真实跑一次选区流程再取消，验证建立期现场与
    取消后清理（蒙版/边框/拦截层全部销毁、登记表清空、`ScreenshotSelOverlays`/取消闭包置零、Esc 需求归零）。
  - `test\Screenshot\test_toolbar_hover.ahk`（21 项）：把悬停/选中态颜色变成可断言数据——向控件父窗口发
    `WM_CTLCOLORSTATIC` 读回背景刷子真实颜色，逐态校验普通/悬停/选中/离开选中的落定色，渐变自停与
    Map 清理、控件销毁后的安全退出、`ClearTransient` 清暂存。
  - 驱动方式一律调用真实回调（`ToolbarToolClick`/`ToolbarOutputClick`/取消闭包），不做鼠标模拟；
    每个用例自带看门狗，结果文件用完即删。

## [0.3.3] - 2026-09-11

标注能力与更新机制。

### 新增

- **文本标注工具**：截图标注支持添加文字，字号跟随线条粗细档位，文本输入框字号按 DPI 换算，与成品渲染一致。
- **GitHub 自动更新**（仅编译版）：
  - 启动时自动检查 GitHub 最新 Release，语义化版本比对、SHA256 校验，下载后延迟替换并自动重启
  - 设置「关于」页新增「启动时自动检查更新」开关与「检查更新」按钮，点击即时显示进度与状态反馈

### 变更

- **输入状态指示器跟手**：位置刷新间隔收紧至 10ms，跟随更顺滑；光标不动时零位移开销。
- **指示器淡入淡出**：显示/隐藏增加透明度过渡，消除突然出现与消失的突兀感。

## [0.3.2] - 2026-09-09

编辑与标注稳定性优化。

### 修复

- **IME 检测整体兜底**：指示器定时器回调对三次 DllCall 检测加 try/catch，异常时兜底为「不确定」状态自然退化，避免定时器反复弹错误框刷屏。

### 变更

- **马赛克改为笔触式**：由矩形框选改为沿轨迹涂抹的固定格网像素化，笔画粗细（细/中/粗）跟随线条档位，缓解不规则断裂。
- **画笔/马赛克小圆圈光标**：改用 32×32 真透明小圆圈，深浅背景均清晰。
- **线宽不持久化**：每次编辑会话重置为默认中档，不再保留上次选择。

## [0.3.1] - 2026-09-07

截图工具栏重做（本版本未推送 tag／发布，内容随 0.3.2 一同发布）。

### 变更

- **选区与编辑工具栏合并**：改为跨阶段持久复用同一套工具栏，选区 → 编辑过渡不再重建、无跳变。
- **工具栏改版**：纯图标 + 尺寸缩小 + 悬停提示（tooltip）。
- **颜色栏过渡动画**：颜色栏出现时增加过渡，去掉左侧竖线。

## [0.3.0] - 2026-09-04

桌面快捷方式与发布流程调整。

### 新增

- **桌面快捷方式**：设置窗口可一键创建/删除桌面 `ZestCaps.lnk`（默认关闭）。
- README 补充 0.2.1 的定时清空回收站说明。

### 工程

- 发布策略调整：打标签直接正式发布（不再先存草稿），CI 自动把 CHANGELOG 对应版本段落填为 Release 正文。
- 规则新增开源项目提交隐私检查（禁止提交个人路径/密钥/邮箱等）。

## [0.2.1] - 2026-09-02

新增回收站定时清理功能。

### 新增

- **回收站定时清空**：设置窗口可配置定时清理回收站
  - 可设置执行时刻，使用时分微调时间控件，上下微调或直接输入 24 时制
  - 到点自动清空回收站，便于无人值守时的磁盘空间维护

## [0.1.1] - 2026-09-01

首版发布后的图标与文档调整。

### 变更

- **程序图标**：改为「循环环」方案（`resources/capslock.ico`），并新增矢量源 `resources/zestcaps.svg`。

### 工程

- 新增本 CHANGELOG，开始按版本记录变更。

## [0.1.0] - 2026-09-01

首版开源发布。

### 新增

- **CapsLock 键增强**：短按切换中英文输入法，长按切换 CapsLock 大小写
- **输入状态指示器**：光标旁实时显示 `中 / 英 / A`
  - 基于 `WM_IME_CONTROL` 消息实时检测真实状态，兼容微信输入法等 TSF 输入法
  - 按窗口独立记忆输入法状态，切换窗口自动恢复
  - 状态缓存 + 变化时才重绘，防闪烁、低资源占用
- **纯文本粘贴**（`Ctrl+Shift+V`，可配置）：粘贴时自动去除剪贴板格式与首尾空白
- **区域截图**（`F1`，可配置）：
  - 灰色半透明蒙版挖洞高亮选区 + 天蓝边框，悬停自动吸附窗口
  - 双击快速复制，或经工具栏标注/保存/钉屏；`Esc` / 右键 / 超时自动取消
  - 标注编辑窗：矩形 / 箭头 / 椭圆 / 马赛克，支持清除、复制、保存、钉屏
- **启动闪屏动画**：暗色圆角卡片，`中/英/A` 芯片循环点亮，淡入淡出自动关闭（GDI+ 分层窗口，纯代码绘制）
- **开机自启**：设置窗口一键开关
- **设置窗口 + 托盘菜单**：功能开关与可配置快捷键集中管理

### 工程

- 基于 AutoHotkey v2，绿色免安装；`build.bat` 可编译为独立 `zestcaps.exe`
- 全局未捕获错误处理与调试日志，配备按键看门狗防假死

[Unreleased]: https://github.com/tt22yui/zestcaps/compare/v0.3.4...HEAD
[0.3.4]: https://github.com/tt22yui/zestcaps/releases/tag/v0.3.4
[0.3.3]: https://github.com/tt22yui/zestcaps/releases/tag/v0.3.3
[0.3.2]: https://github.com/tt22yui/zestcaps/releases/tag/v0.3.2
[0.3.0]: https://github.com/tt22yui/zestcaps/releases/tag/v0.3.0
[0.2.1]: https://github.com/tt22yui/zestcaps/releases/tag/v0.2.1
[0.1.1]: https://github.com/tt22yui/zestcaps/releases/tag/v0.1.1
[0.1.0]: https://github.com/tt22yui/zestcaps/releases/tag/v0.1.0